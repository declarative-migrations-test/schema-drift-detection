# schema-drift-detection

    Independent **database conformance** harness in `declarative-migrations-test` for `declarative-migrations`.

    **Readiness:** `ready`  
    **Primary dependency strategy:** `matrix`  
    **Scheduled cadence:** `23 4 * * 2,5` UTC  
    **Live infrastructure:** PostgreSQL, CockroachDB

    ## Upstream repositories

    - `declarative-migrations/declarative-postgres-migrate.rs`

    ## Acceptance objectives

    1. Verify out-of-band tables, columns, indexes, constraints, defaults, and deterministic remediation across the supported happy-path states and canonical fixtures.
2. Verify out-of-band tables, columns, indexes, constraints, defaults, and deterministic remediation under retries, interruption, concurrency, offline operation, or partial failure.
3. Verify out-of-band tables, columns, indexes, constraints, defaults, and deterministic remediation preserves authorization, idempotency, integrity, observability, and actionable failure classification.

Every behavior change must add a regression, preserve exact dependency pinning, avoid credentials in source or logs, and land through a pull request.
<!-- generated by zed-pkg-test/test-org-fleet; edit the fleet manifest or bootstrapper, not this file. -->
# declarative-migrations-test/schema-drift-e2e

Generated migration-e2e harness for declarative-migrations.

## Contract

Profile: `migration-e2e`  
Source organization: `declarative-migrations`

- manual drift detection
- destructive-change policy
- rename ambiguity
- deterministic plan

## Immutable source pins

| Source | Commit | Branch observed |
|---|---:|---|
| `declarative-migrations/declarative-postgres-migrate.rs` | `c1e0d15c08d55d52909b6eb2b2d5290529ce0114` | `main` |

## Dependency lanes

| Mode | Source | Manager | Path/subdir |
|---|---|---|---|
| _none_ | - | - | - |

## Running

The pull-request workflow validates the generated contract without cross-organization credentials. Full integration is intentionally release-gated until required source repositories and organization read credentials are present. Run the profile-specific checks recorded in `test-plan.json` after materializing the submodule, Zed, or native-package lane.

    ## Dependency paths

    This repository tests the upstream through independent installation paths:

    1. `./scripts/bootstrap-upstream.sh git-submodule`
    2. `./scripts/bootstrap-upstream.sh zed`
    3. `./scripts/bootstrap-upstream.sh native-package`

    The publisher materializes a real Git submodule when authenticated access is available. Zed and native package coordinates are recorded in `dependency-contract.yaml`; missing unpublished packages are reported as blocked readiness rather than silently skipped.

    ## Check tiers

    ```bash
    python3 -m pip install -e '.[test]'
    pytest -q
    ./scripts/readiness.py --offline
    ./scripts/run-live.sh
    ```

    Pull requests validate the harness and deterministic contract fixtures. Secret-, service-, emulator-, desktop-, database-, provider-, chaos-, scale-, and soak-dependent checks run by schedule or manual dispatch.

    A live result must be classified as one of:

    - **product regression** — a behavioral invariant fails after dependencies are ready;
    - **blocked dependency** — an upstream, credential, package, emulator, provider sandbox, or deployment is unavailable;
    - **harness regression** — generated metadata, fixtures, workflow, or runner setup is invalid.

    Managed by `github-test-org-factory/1.0.0`.
