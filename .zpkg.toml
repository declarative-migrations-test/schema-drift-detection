[package]
org = "declarative-migrations-test"
name = "schema-drift-detection"
version = "0.1.0"
description = "Independent database acceptance harness"
license = "MIT"

[package.repository]
vcs = "git"
url = "https://github.com/declarative-migrations-test/schema-drift-detection"

[install]
dir = ".vendor/.zed"

[publish]
exclude = ["artifacts/**", "vendor/**", ".vendor/**", ".env", ".env.*"]
